package base;


/**
 *  Basic int wrapper so the status bars can update accordingly.
 */
// NOTE: I tried using Integer as a wrapper, it didn't produce the results I wanted.
public class DayHolder {
    public int day;
}
